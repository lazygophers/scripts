#!/usr/bin/env python3
"""vpn-prio — 调整 macOS 网络服务优先级（压低 OpenVPN 推送路由的 default 出口）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.vpn_prio import main

raise SystemExit(main())
