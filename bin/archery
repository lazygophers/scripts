#!/usr/bin/env python3
"""archery — Archery(hhyo/archery) 的命令行客户端，按域名分别保存登录"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.archery import main

raise SystemExit(main())
