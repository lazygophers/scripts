#!/usr/bin/env python3
"""switch_branch — 批量切换分支（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.switch_branch import main

raise SystemExit(main())
