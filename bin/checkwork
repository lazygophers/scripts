#!/usr/bin/env python3
"""checkwork 薄壳入口 — 自动化编译检查。"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.build import run_checkwork
from lib.notify import consume_no_say
from lib.ui import timed

sys.argv = consume_no_say(sys.argv)
raise SystemExit(timed(run_checkwork, label="checkwork")())
