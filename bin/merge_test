#!/usr/bin/env python3
"""merge_test 薄壳入口 — merge 当前分支 → 目标分支 test"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.gitwf import merge_test as main

raise SystemExit(main())
