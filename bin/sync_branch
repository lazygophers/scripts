#!/usr/bin/env python3
"""sync_branch — 批量同步分支到 origin/<branch>（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.sync_branch import main

raise SystemExit(main())
