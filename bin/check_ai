#!/usr/bin/env python3
"""check_ai — AI API 端点连通性检测（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.check_ai import main

raise SystemExit(main())
