#!/usr/bin/env python3
"""kkp — 按端口号终止占用进程（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.kkp import main

raise SystemExit(main())
