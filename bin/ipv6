#!/usr/bin/env python3
"""ipv6 薄壳入口 — 开关本机所有网络服务的 IPv6（enable/disable 子命令，fire）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.ipv6 import main

raise SystemExit(main())
