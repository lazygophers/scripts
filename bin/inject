#!/usr/bin/env python3
"""inject — 把当前仓库 bin/ 注入 shell PATH，并安装 zsh/bash/fish completion（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.inject import main

raise SystemExit(main())
