#!/usr/bin/env python3
"""issue — 自动创建 Issue（调 claude 生成 title/body）（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.issue import main

raise SystemExit(main())
