#!/usr/bin/env python3
"""mr — 自动创建 PR/MR（调 claude 生成 title/body）（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.mr import main

raise SystemExit(main())
