#!/usr/bin/env python3
"""graphwatch — graphify 全局 watch 守护服务：注册目录，后台自动重建知识图谱。"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.graphwatch import main

raise SystemExit(main())
