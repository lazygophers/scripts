#!/usr/bin/env python3
"""ovpn — 连 OpenVPN，自动填账号密码和二步验证码"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.ovpn import main

raise SystemExit(main())
