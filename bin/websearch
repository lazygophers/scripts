#!/usr/bin/env python3
"""websearch 薄壳入口 — 多引擎检索,结果打 stdout。"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.websearch import main

raise SystemExit(main())
