#!/usr/bin/env python3
"""loop — 循环执行命令并追踪结果（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.loop import main

raise SystemExit(main())
