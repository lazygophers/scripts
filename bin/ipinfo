#!/usr/bin/env python3
"""ipinfo — 内网 IP + 网络类型"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.ipinfo import main

raise SystemExit(main())
