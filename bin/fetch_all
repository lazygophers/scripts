#!/usr/bin/env python3
"""fetch_all — 一键拉取所有仓库远程更新（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.fetch_all import main

raise SystemExit(main())
