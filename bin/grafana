#!/usr/bin/env python3
"""grafana — Grafana HTTP API 命令行客户端，按域名分别保存登录。"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.grafana import main

raise SystemExit(main())
