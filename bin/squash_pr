#!/usr/bin/env python3
"""squash_pr — 把 source 自分叉以来的改动压成单 commit → 对接 mr 开 PR（fire 重构）"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from lib.cli.squash_pr import main

raise SystemExit(main())
