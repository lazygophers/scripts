# الاختبار

```bash
python3 -m unittest discover -s tests -q
```
المجموعة في `tests/` بجذر المستودع. الأوامر الجديدة يُنصح بإضافة اختبارات: يُختبر المنطق في `lib/{الاسم}.py`؛ المداخل الرقيقة تمرر فقط. ‏`tests/test_lazyhelp.py` يتحقق من تطابق bin/ مع سجل `TOOLS` — تذكّر تسجيل المداخل الجديدة.
