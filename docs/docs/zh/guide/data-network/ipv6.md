# ipv6

开关本机所有网络服务的 IPv6（enable/disable 子命令，需 sudo）

## 用法

用法：`ipv6 COMMAND`

**命令**

| 命令 | 说明 |
| :--- | :--- |
| `disable` | 关闭本机所有网络服务的 IPv6 |
| `enable` | 开启本机所有网络服务的 IPv6（自动模式） |

## 示例

```bash
sudo ipv6 disable
sudo ipv6 enable
```
