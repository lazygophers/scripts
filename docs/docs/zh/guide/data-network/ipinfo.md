# ipinfo

查询内网 IP + 网络类型（含热点识别）

## 用法

用法：`ipinfo COMMAND | [NAMES]...`

**命令**

| 命令 | 说明 |
| :--- | :--- |
| `all` | 一次性展示全部（默认入口） |
| `lan` | 仅显示本机内网 IP |
| `net` | 仅显示网络类型（逗号分隔多个活动接口） |

**参数 / 选项**

| 参数 | 类型 | 默认值 |
| :--- | :--- | :--- |
| `NAMES` | 'str' |  |

## 示例

```bash
ipinfo
```
