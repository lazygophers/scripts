# switch_branch

批量切换所有仓库到指定分支（不存在则从默认主分支创建）

## 用法

用法：`switch_branch COMMAND | [ARGS]...`

**命令**

| 命令 | 说明 |
| :--- | :--- |
| `to` | 切换到指定分支 |

**参数 / 选项**

| 参数 | 类型 | 默认值 |
| :--- | :--- | :--- |
| `ARGS` | 'str' |  |

## 示例

```bash
switch_branch <branch>
```
