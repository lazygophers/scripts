# sync_master

批量同步各仓库默认主分支（master/main 自动识别）

## 用法

用法：`sync_master COMMAND | <flags>`

**命令**

| 命令 | 说明 |
| :--- | :--- |
| `run` | 同步主分支到 origin/<主分支> |

**参数 / 选项**

| 参数 | 类型 | 默认值 |
| :--- | :--- | :--- |
| `-f, --force=FORCE` | 'bool' | False |

## 示例

```bash
sync_master
```
